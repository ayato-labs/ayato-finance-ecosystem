[project]
name = "forex"
version = "0.1.0"
description = "Forex Service for Ayato Finance Ecosystem"
dependencies = [
    "fastapi",
    "uvicorn",
    "yfinance",
    "pandas",
    "duckdb",
    "pyarrow",
    "python-dotenv",
    "coloredlogs",
    "loguru>=0.7.3",
]

[tool.uv]
dev-dependencies = [
    "pytest",
    "ruff",
]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "B", "UP", "PL", "RUF"]
ignore = ["PLR0913"]
