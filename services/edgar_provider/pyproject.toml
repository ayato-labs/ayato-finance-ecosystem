[project]
name = "edgar-provider-workspace"
version = "0.1.0"
description = "Monorepo for EDGAR Provider components"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[tool.uv.workspace]
members = ["libs/*", "apps/*"]

[tool.uv]
dev-dependencies = [
    "pytest>=8.0.0",
]
