# Database Schema Documentation

This document is automatically generated from `src/core/contracts.py` (Schema-as-Code).
Do not edit this file manually. Update the Pydantic models in the contracts file instead.

## Governance Rules
- **SSoT**: `src/core/contracts.py` is the single source of truth.
- **Migrations**: Sequential versioning via `schema_migrations` table.
- **Registry**: `master.duckdb` tracks all distributed shards.

### Table: `metrics`
> Execution metrics and tracing for pipeline steps.

**Primary Key**: `None`

| Column | Type | Description |
| --- | --- | --- |
| `run_id` | `VARCHAR` | Unique identifier for the execution run |
| `step_name` | `VARCHAR` | Name of the pipeline step |
| `ticker` | `VARCHAR` | Ticker symbol being processed |
| `latency_ms` | `DOUBLE` | Execution time in milliseconds |
| `status` | `VARCHAR` | Status of the step (success, failed) |
| `error_log` | `VARCHAR` | Error message if failed |
| `inputs` | `VARCHAR` | JSON string of inputs |
| `outputs` | `VARCHAR` | JSON string of outputs |
| `recorded_at` | `TIMESTAMP` | Timestamp of recording |

### Table: `processed_companies`
> State tracking for ingested companies.

**Primary Key**: `ticker`

| Column | Type | Description |
| --- | --- | --- |
| `ticker` | `VARCHAR` | Ticker symbol of the company |
| `cik` | `VARCHAR` | CIK identifier |
| `status` | `VARCHAR` | Current ingestion status (completed, failed) |
| `last_processed_at` | `TIMESTAMP` | Last update timestamp |
| `error_log` | `VARCHAR` | Error message if failed |

### Table: `tickers`
> Metadata for SEC registered companies.

**Primary Key**: `ticker`

| Column | Type | Description |
| --- | --- | --- |
| `ticker` | `VARCHAR` | Stock ticker symbol |
| `cik` | `VARCHAR` | Central Index Key (10 digits) |
| `name` | `VARCHAR` | Company legal name |
| `exchange` | `VARCHAR` | Stock exchange name |
| `last_session_id` | `VARCHAR` | Session ID that ingested this record |
| `ingested_at` | `TIMESTAMP` | Timestamp of ingestion |

### Table: `company_facts`
> Standardized financial facts (XBRL).

**Primary Key**: `ticker, accession_number, label`

| Column | Type | Description |
| --- | --- | --- |
| `ticker` | `VARCHAR` | Stock ticker symbol |
| `cik` | `VARCHAR` | Central Index Key |
| `accession_number` | `VARCHAR` | Filing accession number |
| `form` | `VARCHAR` | Filing form type (e.g., 10-K, 10-Q) |
| `filed_date` | `DATE` | Date the filing was submitted |
| `fiscal_year` | `SMALLINT` | Fiscal year of the statement |
| `fiscal_period` | `VARCHAR` | Fiscal period (e.g., Q1, FY) |
| `label` | `VARCHAR` | Standardized fact label (e.g., NetIncome) |
| `value` | `DOUBLE` | Numeric value of the fact |
| `unit` | `VARCHAR` | Unit of measurement (e.g., USD) |
| `is_standardized` | `BOOLEAN` | Whether the label is normalized |
| `raw_tag` | `VARCHAR` | Original XBRL tag for traceability |
| `session_id` | `VARCHAR` | Session ID that ingested this record |
| `ingested_at` | `TIMESTAMP` | Timestamp of ingestion |

### Table: `narratives`
> Extracted text narratives from filings.

**Primary Key**: `ticker, accession_number, section_name`

| Column | Type | Description |
| --- | --- | --- |
| `ticker` | `VARCHAR` | Stock ticker symbol |
| `cik` | `VARCHAR` | Central Index Key |
| `accession_number` | `VARCHAR` | Filing accession number |
| `form` | `VARCHAR` | Filing form type (e.g., 10-K, 10-Q) |
| `filed_date` | `DATE` | Date the filing was submitted |
| `section_name` | `VARCHAR` | Extracted section (e.g., 'Risk Factors', 'MD&A') |
| `content_md_zstd` | `BLOB` | Zstandard compressed markdown content |
| `session_id` | `VARCHAR` | Session ID that ingested this record |
| `ingested_at` | `TIMESTAMP` | Timestamp of ingestion |

### Table: `databases`
> Registry of distributed database files.

**Primary Key**: `db_id`

| Column | Type | Description |
| --- | --- | --- |
| `db_id` | `VARCHAR` | Unique identifier for the database shard (e.g., 'edgar_2024') |
| `file_path` | `VARCHAR` | Relative or absolute path to the DuckDB file |
| `role` | `VARCHAR` | Role of this DB (e.g., 'facts', 'narratives', 'master') |
| `schema_version` | `VARCHAR` | Current schema version of this DB |
| `created_at` | `TIMESTAMP` | Creation timestamp |

### Table: `data_catalog`
> Catalog mapping data partitions to database shards.

**Primary Key**: `partition_key`

| Column | Type | Description |
| --- | --- | --- |
| `partition_key` | `VARCHAR` | Key describing the partition (e.g., 'AAPL_2024') |
| `db_id` | `VARCHAR` | Identifier of the database shard holding this partition |
| `description` | `VARCHAR` | Human-readable description of the partition |
| `updated_at` | `TIMESTAMP` | Last update timestamp |
